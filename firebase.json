{
  "firestore": [
    {
      "database": "(default)",
      "rules": "firestore.rules",
      "indexes": "firestore.indexes.json"
    },
    {
      "database": "ai-powered-timetable-generator",
      "rules": "firestore.rules",
      "indexes": "firestore.indexes.json"
    }
  ]
}

