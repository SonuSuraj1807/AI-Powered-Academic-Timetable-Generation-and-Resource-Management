{
  "name": "vbit-timetable-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for VBIT Timetable System",
  "main": "src/index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
